# sbatch_prototype
This is a SLURM batch job prototype for running Python scripts on the Flatiron Institute HPC cluster. 

helloworld.sh executes a very basic python script on the default partition. 

train_simple_network.sh requests a gpu and tests by training a simple neural network using pytorch.
